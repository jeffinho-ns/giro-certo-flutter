/// Sessão da corrida dedicada (`TripNavigationScreen`).
class TripNavigationExperiment {
  TripNavigationExperiment._();

  /// Corrida ativa na tela dedicada (evita pipcar duplicado na Home).
  static bool activeSessionOpen = false;
}
